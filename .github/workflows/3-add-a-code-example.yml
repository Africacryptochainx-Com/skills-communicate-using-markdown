

```yaml
name: Step 3, Add a code example

# This step triggers after a commit on the branch `start-markdown`.
# This workflow updates from step 3 to step 4.

# This will run every time we create push a commit to `start-markdown`.
on:
  workflow_dispatch:
  push:
    branches:
      - start-markdown

# Permissions required for the workflow
permissions:
  # Need `contents: read` to checkout the repository.
  # Need `contents: write` to update the step metadata.
  contents: write

jobs:
  # Get the current step to only run the main job when the learner is on the same step.
  get_current_step:
    name: Check current step number
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - id: get_step
        run: |
          echo "current_step=$(cat ./.github/steps/-step.txt)" >> $GITHUB_OUTPUT
    outputs:
      current_step: ${{ steps.get_step.outputs.current_step }}

  on_commit_start_markdown_code_example:
    name: On commit, start markdown, code example
    needs: get_current_step

    # Conditional execution:
    # 1. This repository isn't the template repository.
    # 2. The step is currently 3.
    if: >-
      ${{ !github.event.repository.is_template
          && needs.get_current_step.outputs.current_step == 3 }}

    runs-on: ubuntu-latest

    steps:
      # Check out the repository to edit the README.
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Retrieve all branches.

      # Ensure there is at least one code example in the markdown file.
      - name: Check markdown syntax, code example
        uses: skills/action-check-file@v1
        with:
          file: "index.md"
          search: "```"

      # Update README.md to reflect step 4 instead of step 3.
      - name: Update to step 4
        uses: skills/action-update-step@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          from_step: 3
          to_step: 4
          branch_name: start-markdown

      # Incorporate project information into README.md
      - name: Include project information
        run: |
          echo "```markdown" >> index.md
          echo "# AfricaCryptoChainx.Com CI Workflow and Project Information" >> index.md
          echo "" >> index.md
          echo "_Transforming financial inclusion and sustainability through secure, accessible, and innovative financial services._" >> index.md
          echo "" >> index.md
          echo "## Welcome" >> index.md
          echo "" >> index.md
          echo "Welcome to AfricaCryptoChainx, a revolutionary initiative aimed at transforming the financial landscape in Africa by seamlessly integrating traditional banking systems with cutting-edge blockchain technology. Our mission is to provide secure, accessible, and inclusive financial services, fostering innovation, collaboration, and sustainable development across the continent." >> index.md
          echo "" >> index.md
          echo "### Audience" >> index.md
          echo "" >> index.md
          echo "This guide is designed for developers, blockchain enthusiasts, and financial technology innovators interested in implementing a robust CI (Continuous Integration) workflow, securing infrastructure, and effectively managing project milestones." >> index.md
          echo "" >> index.md
          echo "### Learning Objectives" >> index.md
          echo "" >> index.md
          echo "By following this comprehensive guide, you will gain the necessary knowledge and skills to build a secure, intuitive, and inclusive financial platform equipped with decentralized finance (DeFi) functionalities." >> index.md
          echo "" >> index.md
          echo "### Prerequisites" >> index.md
          echo "" >> index.md
          echo "To maximize your learning experience, familiarity with GitHub, CI/CD concepts, and basic Python programming is recommended. However, detailed instructions are provided to help you grasp the fundamentals effectively." >> index.md
          echo "" >> index.md
          echo "### Time Commitment" >> index.md
          echo "" >> index.md
          echo "You can complete this guide within just a few hours, enabling you to quickly start implementing the strategies and practices outlined here." >> index.md
          echo "" >> index.md
          echo "## Getting Started" >> index.md
          echo "" >> index.md
          echo "To begin, clone the AfricaCryptoChainx.Com repository and follow the step-by-step instructions provided in the README file of the repository." >> index.md
          echo "" >> index.md
          echo "### AfricaCryptoChainx.Com CI Workflow" >> index.md
          echo "" >> index.md
          echo "#### Creating \`blank.yml\`" >> index.md
          echo "" >> index.md
          echo "Setting up a CI workflow is essential for automating build, test, and deployment processes. Below is a basic example of a GitHub Actions workflow configuration (\`blank.yml\`) that can be customized according to your project's needs:" >> index.md
          echo "" >> index.md
          echo "```yaml" >> index.md
          echo "name: CI" >> index.md
          echo "" >> index.md
          echo "on:" >> index.md
          echo "  push:" >> index.md
          echo "    branches: [ \"main\" ]" >> index.md
          echo "  pull_request:" >> index.md
          echo "    branches: [ \"main\" ]" >> index.md
          echo "  workflow_dispatch:" >> index.md
          echo "" >> index.md
          echo "jobs:" >> index.md
          echo "  build:" >> index.md
          echo "    runs-on: ubuntu-latest" >> index.md
          echo "    steps:" >> index.md
          echo "      - uses: actions/checkout@v4" >> index.md
          echo "      - name: Run a one-line script" >> index.md
          echo "        run: echo Hello, world!" >> index.md
          echo "      - name: Run a multi-line script" >> index.md
          echo "        run: |" >> index.md
          echo "          echo Add other actions to build," >> index.md
          echo "          echo test, and deploy your project." >> index.md
          echo "```" >> index.md
          echo "" >> index.md
          echo "#### Project Information: AfricaCryptoChainx.Com" >> index.md
          echo "" >> index.md
          echo "![GitHub license](https://img.shields.io/github/license/skills-communicate-using-markdown)" >> index.md
          echo "![GitHub issues](https://img.shields.io/github/issues/skills-communicate-using-markdown.Com)" >> index.md
          echo "![GitHub forks](https://img.shields.io/github/forks/skills-communicate-using-markdown.Com)" >> index.md
          echo "![GitHub stars](https://img.shields.io/github/stars/skills-communicate-using-markdown.Com)" >> index.md
          echo "![GitHub issues](https://img.shields.io/github/issues/TeachmastermindPat/skills-communicate-using-markdown)" >> index.md
          echo "![GitHub forks](https://img.shields.io/github/forks/TeachmastermindPat/skills-communicate-using-markdown)" >> index.md
          echo "![GitHub stars](https://img.shields.io/github/stars/TeachmastermindPat/skills-communicate-using-markdown)" >> index.md
          echo "" >> index.md
          echo "#### Milestone: AfricaCryptoChainx.Com Version 1.0 Launch" >> index.md
          echo "" >> index.md
          echo "- **Objective**: Our goal is to launch AfricaCryptoChainx.Com by June 30, 2024. This launch will encompass the implementation of secure infrastructure, integration with P2P Networkers, deployment of advanced security measures, development of an intuitive user interface, creation of educational resources, initiation of community building initiatives, and incorporation of DeFi functionalities." >> index.md
          echo "" >> index.md
          echo "#### Key Tasks" >> index.md
          echo "" >> index.md
          echo "- **Documentation**: Create comprehensive user and developer guides." >> index.md
          echo "- **Beta Testing**: Collect valuable feedback from beta testers to refine the platform." >> index.md
          echo "- **Marketing**: Prepare promotional materials and strategies to raise awareness about AfricaCryptoChainx.Com." >> index.md
          echo "- **Access Control**: Establish stringent access control mechanisms to safeguard project accounts and resources." >> index.md
          echo "" >> index.md
          echo "#### Progress Updates" >> index.md
          echo "" >> index.md
          echo "To ensure transparency and accountability, we have outlined the milestones and progress updates for AfricaCryptoChainx.Com:" >> index.md
          echo "" >> index.md
          echo "- **Week 1 (Apr 1-7, 2024)**: Commenced the development of secure infrastructure." >> index.md
          echo "- **Week 2 (Apr 8-14, 2024)**: Initiated the integration with P2P Networkers." >> index.md
          echo "- **Week 3 (Apr 15-21, 2024)**: Implemented advanced security measures to fortify the platform." >> index.md
          echo "- **Week 4 (Apr 22-30, 2024)**:name: Step 3, Add a code example

# This step triggers after a commit on the branch `start-markdown`.
# This workflow updates from step 3 to step 4.

# This will run every time we create push a commit to `start-markdown`.
# Reference: https://docs.github.com/en/actions/learn-github-actions/events-that-trigger-workflows
on:
  workflow_dispatch:
  push:
    branches:
      - start-markdown

# Reference: https://docs.github.com/en/actions/security-guides/automatic-token-authentication
permissions:
  # Need `contents: read` to checkout the repository.
  # Need `contents: write` to update the step metadata.
  contents: write

jobs:
  # Get the current step to only run the main job when the learner is on the same step.
  get_current_step:
    name: Check current step number
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - id: get_step
        run: |
          echo "current_step=$(cat ./.github/steps/-step.txt)" >> $GITHUB_OUTPUT
    outputs:
      current_step: ${{ steps.get_step.outputs.current_step }}

  on_commit_start_markdown_code_example:
    name: On commit, start markdown, code example
    needs: get_current_step

    # We will only run this action when:
    # 1. This repository isn't the template repository.
    # 2. The step is currently 3.
    # Reference: https://docs.github.com/en/actions/learn-github-actions/contexts
    # Reference: https://docs.github.com/en/actions/learn-github-actions/expressions
    if: >-
      ${{ !github.event.repository.is_template
          && needs.get_current_step.outputs.current_step == 3 }}

    # We'll run Ubuntu for performance instead of Mac or Windows.
    runs-on: ubuntu-latest

    steps:
      # We'll need to check out the repository so that we can edit the README.
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Let's get all the branches.

      # Check that there is at least one code example in the markdown file.
      - name: Check markdown syntax, code example
        uses: skills/action-check-file@v1
        with:
          file: "index.md"
          search: "```"

      # In README.md, switch step 3 for step 4.
      - name: Update to step 4
        uses: skills/action-update-step@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          from_step: 3
          to_step: 4
          branch_name: start-markdown
